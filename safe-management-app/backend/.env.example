# Server Configuration
PORT=5000
NODE_ENV=development

# Safe Configuration (Default Session)
# Replace with your actual Safe address
DEFAULT_SAFE_ADDRESS=0x7B389710824aa7e8821bc4ae10f1f7411B8Be04F
CHAIN_ID=1

# Safe Transaction Service URL (auto-updates based on CHAIN_ID)
# Mainnet: https://safe-transaction-mainnet.safe.global
# Sepolia: https://safe-transaction-sepolia.safe.global
TRANSACTION_SERVICE_URL=https://safe-transaction-mainnet.safe.global

# Safe SDK - Company-wide API Key
# This key will be used as a fallback when sessions don't have their own API key
# Optional - only needed if you hit rate limits
COMPANY_SAFE_API_KEY=

# RPC URLs (Optional - will use public RPCs as fallback)
# You can provide your own RPC URLs for better performance
# Sign up at: https://alchemy.com or https://infura.io
ETH_MAINNET_RPC=
SEPOLIA_RPC=
POLYGON_RPC=
ARBITRUM_RPC=
OPTIMISM_RPC=
BASE_RPC=
GNOSIS_RPC=

# Database Configuration
# PostgreSQL (recommended):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/safe_management"
# SQLite (development only):
# DATABASE_URL="file:./dev.db"

# Application URL (for payment links)
APP_URL=http://localhost:3000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
